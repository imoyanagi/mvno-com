<div class="container text-center">
	<h4 class="p-3 mt-4 rounded">格安スマホのプランを検索する</h4>
	<div class="bg-light rounded p-4">
		<div>
			<h5>毎月のデータ使用量を選んでください</h5>
			<!-- 特定のviewだけ水平にする方法もある -->
			<div class="list-group data-list">
				<%= search_form_for(@q) do |f| %>
					<%= f.hidden_field :data_value_lteq, value:1 %>
					<%= f.submit "~1GB", class:"btn list-group-item list-group-item-action"%>
				<% end %>
				<%= search_form_for(@q) do |f| %>
					<%= f.hidden_field :data_value_gteq, value:1 %>
					<%= f.hidden_field :data_value_lteq, value:3 %>
					<%= f.submit "1GB 〜 3GB", class:"btn list-group-item list-group-item-action"%>
				<% end %>
				<%= search_form_for(@q) do |f| %>
					<%= f.hidden_field :data_value_gteq, value:3 %>
					<%= f.hidden_field :data_value_lteq, value:7 %>
					<%= f.submit "3GB 〜 7GB", class:"btn list-group-item list-group-item-action"%>
				<% end %>
				<%= search_form_for(@q) do |f| %>
					<%= f.hidden_field :data_value_gteq, value:7 %>
					<%= f.submit "7GB 〜", class:"btn list-group-item list-group-item-action"%>
				<% end %>
  			</div>
		</div>
		<%= render 'detail' %>
	</div>
	<div class="row mt-5">
		<div class="col-5 mb-5 rounded">
			<div class="rounded bg-light">
				<div class="p-3 ranking"><span>人気ランキング</span></div>
				<% @plans.ranking.each_with_index do |plan, index| %>
					<table class="table">
						<tr>
							<td class="ranking-num align-middle"><%= index + 1 %>位</td>
							<td><%= image_tag("linemobile.png", size:"70x70") %></td>
							<td class="text-left"><%= link_to plan_path(plan) do %><%= plan.carrier.name %><br><%= plan.name %><% end %></td>
						</tr>
					</table>
				<% end %>
			</div>
			<div class="bg-light mt-4 rounded">
				<h4 class="p-3 mb-4 mt-3">最新記事</h4>
				<div class="p-3 clearfix">
					<%= image_tag("linemobile.png", size:"70x70", class:"float-left") %>
					<div class="float-left text-left ml-2">
						<h6 class="text-info">記事のタイトルが入ります</h6>
						<p>記事の本文が入ります。記事の本文が入り<br>記事の本文が入ります。記事の本文が入り</p>
					</div>
				</div>
				<div class="p-3 clearfix">
					<%= image_tag("linemobile.png", size:"70x70", class:"float-left") %>
					<div class="float-left text-left ml-2">
						<h6 class="text-info">記事のタイトルが入ります</h6>
						<p>記事の本文が入ります。記事の本文が入り<br>記事の本文が入ります。記事の本文が入り</p>
					</div>
				</div>
				<div class="p-3 clearfix">
					<%= image_tag("linemobile.png", size:"70x70", class:"float-left") %>
					<div class="float-left text-left ml-2">
						<h6 class="text-info">記事のタイトルが入ります</h6>
						<p>記事の本文が入ります。記事の本文が入り<br>記事の本文が入ります。記事の本文が入り</p>
					</div>
				</div>
			</div>
		</div>
		<div class="col-1"></div>
		<div class="col-6 mb-5">
			<h4>最近のQ&A</h4>
			<div class="mt-3 border p-5 bg-light rounded">
				<% @questions.order("id desc").each do |question| %>
					<%= link_to question_path(question), class:"text-dark" do %>
						<div class="row mb-4 border rounded p-3 questionSelected">
							<div class="col-4 d-flex flex-column">
								<%= image_tag("linemobile.png", size:"80x80", class:"mx-auto") %>
								<h6><%= question.user.name %></h6>
							</div>
							<div class="col-8 text-left">
								<h5 class="text-info"><%= question.title %></h5>
								<h6>カテゴリ: <%= question.sub_category.name %></h6>
								good! <%= question.favorite_posts.count %>件 | 回答 <%= question.answers.count %>件
							</div>
						</div>
					<% end %>
				<% end %>
			</div>
		</div>
	</div>
</div>